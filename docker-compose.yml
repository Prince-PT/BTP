version: '3.8'

services:
  postgres:
    image: postgres:18-alpine
    container_name: rideshare-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: rideshare
      POSTGRES_PASSWORD: rideshare123
      POSTGRES_DB: rideshare_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./infra/init.sql:/docker-entrypoint-initdb.d/init.sql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U rideshare"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Uncomment when Redis is needed for background jobs
  # redis:
  #   image: redis:7-alpine
  #   container_name: rideshare-redis
  #   restart: unless-stopped
  #   ports:
  #     - "6379:6379"
  #   volumes:
  #     - redis_data:/data
  #   healthcheck:
  #     test: ["CMD", "redis-cli", "ping"]
  #     interval: 10s
  #     timeout: 5s
  #     retries: 5

volumes:
  postgres_data:
    driver: local
  # redis_data:
  #   driver: local

networks:
  default:
    name: rideshare-network
